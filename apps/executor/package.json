{
  "name": "@steward/executor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "run-test": "bun run --env-file=../../.env tests/tier2.test.ts"
  },
  "dependencies": {
    "@steward/shared-types": "workspace:*",
    "@steward/contracts-sdk": "workspace:*",
    "ethers": "^6.13.0",
    "pg": "^8.12.0",
    "node-cron": "^3.0.3",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "@types/pg": "^8.11.6",
    "typescript": "^5.5.0"
  }
}